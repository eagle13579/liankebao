# ============================================================
# 链客宝AI — Namespace
# ============================================================
apiVersion: v1
kind: Namespace
metadata:
  name: chainke
  labels:
    app: chainke
    name: chainke
    environment: production
    team: chainke-dev
    managed-by: kustomize
