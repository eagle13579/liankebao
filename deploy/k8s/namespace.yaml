# ============================================================
# 链客宝 — Namespace
# ============================================================
apiVersion: v1
kind: Namespace
metadata:
  name: chainke
  labels:
    app: chainke
    name: chainke
