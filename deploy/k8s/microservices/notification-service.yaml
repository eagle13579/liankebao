apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: chainke
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: liankebao/notification-service:latest
        ports:
        - containerPort: 8000
        env:
        - name: DB_TYPE
          value: "sqlite"
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: chainke
spec:
  selector:
    app: notification-service
  ports:
  - port: 8000
    targetPort: 8000
