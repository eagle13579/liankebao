# 链客宝AI — 向海容待办清单：支付凭证/商户号/API密钥注册

> 分析基准: `D:/链客宝AI/.env` + `D:/链客宝AI/.env.example` + `D:/链客宝AI/deploy/.env.example`
> 代码引用: `backend/app/wechat_pay.py`, `backend/payment/config.py`, `backend/app/routers/auth.py`

---

## 一、现状摘要

当前 `.env` 中 **36 个可配置变量**，仅 8 个有值（JWT密钥、CORS、本地URL等开发项），**其余 28 个均为空字符串**。关键支付链路全部处于 mock 模式。

---

## 二、需海容手动注册的完整清单

### 阶段一：微信支付（必须 — 小程序上线必备）

| # | 变量名 | 当前值 | 注册内容 | 在哪注册 | 操作要点 | 预计耗时 | 优先级 |
|---|---|:---:|---|---|---|:---:|:---:|
| 1 | WXPAY_MCHID | "" | **微信支付商户号**（10位数字） | [pay.weixin.qq.com](https://pay.weixin.qq.com) → 注册微信支付商户号 | 准备营业执照、法人身份证、对公账户/法人银行卡，选择「普通商户→小程序」，绑定 AppID `wxb4f6d89904200fd2` | **1-3工作日**（审核） | **P0 必须** |
| 2 | WXPAY_API_V3_KEY | "" | **APIv3 密钥**（32位随机字符串） | pay.weixin.qq.com → 账户中心 → API安全 → APIv3密钥 → 设置密钥 | 用 `openssl rand -hex 16` 生成 32位密钥，**只显示一次，立刻保存** | **5分钟** | **P0 必须** |
| 3 | WXPAY_CERT_SERIAL_NO | "" | **商户证书序列号**（16进制字符串） | pay.weixin.qq.com → 账户中心 → API安全 → 证书管理 → 查看序列号 | 也可 `openssl x509 -in apiclient_cert.pem -noout -serial` | **5分钟** | **P0 必须** |
| 4 | WXPAY_CERT_PATH | "" | **商户API私钥文件** (`apiclient_key.pem`) | pay.weixin.qq.com 下载 `cert.zip` 解压得到 | 复制到 `D:/链客宝AI/backend/certs/apiclient_key.pem`，**禁止提交到Git** | **5分钟** | **P0 必须** |
| 5 | WXPAY_PLATFORM_CERT_PATH | "" | **微信平台证书** (`platform_cert.pem`) | 代码首次回调时自动拉取缓存 / 或手动从商户平台下载 | 复制到 `D:/链客宝AI/backend/certs/platform_cert.pem` | **5分钟** | **P0 必须** |
| 6 | WXPAY_APPID | "" | **小程序 AppID** | [mp.weixin.qq.com](https://mp.weixin.qq.com) → 开发 → 开发管理 → 查看 AppID | 链客宝AI已注册的 AppID = `wxb4f6d89904200fd2`，**直接填入即可** | **1分钟** | **P0 必须** |
| 7 | WXPAY_NOTIFY_URL | "" | **支付回调通知 URL**（公网HTTPS） | 填写你的服务器域名 | 生产: `https://www.go-aiport.com/lkapi/wechat/pay/notify`（已有记录） | **1分钟**（填值） | **P0 必须** |
| 8 | WXPAY_REFUND_NOTIFY_URL | "" | **退款回调通知 URL** | 同上 | 生产: `https://www.go-aiport.com/lkapi/wechat/refund/notify` | **1分钟**（填值） | **P1 建议** |

### 阶段二：微信小程序登录（必须 — 用户登录功能）

| # | 变量名 | 当前值 | 注册内容 | 在哪注册 | 操作要点 | 预计耗时 | 优先级 |
|---|---|:---:|---|---|---|:---:|:---:|
| 9 | WECHAT_APP_SECRET | 未在.env中 | **小程序 AppSecret**（32位字母数字） | mp.weixin.qq.com → 开发 → 开发管理 → 生成/重置 AppSecret | 生成后复制保存，**只显示一次**。代码中 `auth.py` 直接从 `.env` 读取 `WECHAT_APP_SECRET=` | **5分钟** | **P0 必须** |
| 10 | API_SECRET | "" | 同 WECHAT_APP_SECRET（兼容层） | 同上，值相同 | 两个变量填同一个值即可 | **1分钟** | **P1 建议** |
| 11 | WEIXIN_APPID | "" | 同 WXPAY_APPID（V2兼容层） | 同上 | 填 `wxb4f6d89904200fd2` | **1分钟** | **P1 建议** |

### 阶段三：V2 兼容层（可选，建议一起填）

| # | 变量名 | 当前值 | 注册内容 | 在哪注册 | 操作要点 | 预计耗时 | 优先级 |
|---|---|:---:|---|---|---|:---:|:---:|
| 12 | MCH_ID | "" | 同 WXPAY_MCHID | 同阶段一 #1 | 填同一个商户号 | **1分钟** | **P2 建议** |
| 13 | API_KEY | "" | **V2 签名密钥**（32位） | pay.weixin.qq.com → 账户中心 → API安全 → 密钥设置 | 区别于 V3 密钥，部分旧代码可能用到 | **5分钟** | **P2 建议** |
| 14 | NOTIFY_URL | "" | 同 WXPAY_NOTIFY_URL | 填值即可 | 填回调 URL | **1分钟** | **P2 建议** |

### 阶段四：第三方 AI API（推荐）

| # | 变量名 | 当前值 | 注册内容 | 在哪注册 | 操作要点 | 预计耗时 | 优先级 |
|---|---|:---:|---|---|---|:---:|:---:|
| 15 | GEMINI_API_KEY | "" | **Google Gemini AI API 密钥** | [aistudio.google.com](https://aistudio.google.com/) → 创建 API Key | 免费额度足够开发测试 | **5分钟** | **P2 推荐** |
| 16 | DEEPSEEK_API_KEY | 未在.env（在deploy） | **DeepSeek API 密钥** | [platform.deepseek.com](https://platform.deepseek.com/) → API Keys | 用于数据导入/智能处理（`importer.py` 使用） | **5分钟** | **P2 推荐** |

### 阶段五：监控（可选）

| # | 变量名 | 当前值 | 注册内容 | 在哪注册 | 操作要点 | 预计耗时 | 优先级 |
|---|---|:---:|---|---|---|:---:|:---:|
| 17 | SENTRY_DSN | "" | **Sentry 项目 DSN** | [sentry.io](https://sentry.io/) → 创建项目 → 获取 DSN | 免费额度每月 5万 events | **10分钟** | **P3 可选** |

### 阶段六：邮件通知（可选）

| # | 变量 | 当前值 | 注册内容 | 在哪注册 | 操作要点 | 预计耗时 | 优先级 |
|---|---|:---:|---|---|---|:---:|:---:|
| 18-22 | SMTP_HOST / PORT / USER / PASSWORD / FROM | 全空 | **SMTP 邮件服务** | 任意邮件服务商（QQ邮箱/阿里邮件/等） | 建议用 SendGrid 或阿里云邮件推送 | **10分钟** | **P3 可选** |

### 阶段七：支付宝（当前未启用）

| # | 变量 | 当前值 | 注册内容 | 在哪注册 | 操作要点 | 预计耗时 | 优先级 |
|---|---|:---:|---|---|---|:---:|:---:|
| 23-26 | ALIPAY_APP_ID / PRIVATE_KEY / PUBLIC_KEY / NOTIFY_URL | 未在.env | **支付宝商户号 + 公私钥** | [open.alipay.com](https://open.alipay.com/) → 创建应用 | 需企业资质，推荐二期再做 | **3-5工作日** | **P4 二期** |

### 阶段八：数据库（当前用 SQLite 无需操作）

| # | 变量 | 当前值 | 注册内容 | 在哪注册 | 操作要点 | 预计耗时 | 优先级 |
|---|---|:---:|---|---|---|:---:|:---:|
| 27-31 | PG_HOST / PORT / USER / PASSWORD / DATABASE | 全空 | **PostgreSQL 数据库** | 云厂商自建（阿里云RDS等） | **开发阶段用 SQLite 即可**，无需海容操作 | — | **P4 上线前** |

---

## 三、核心依赖关系

```
微信支付上线依赖链（必须全部完成）:
  [小程序注册] ── 已存在 AppID: wxb4f6d89904200fd2
       │
       ▼
  [1] 注册微信支付商户号 ← 等待1-3工作日审核 ← 这是最耗时的步骤！！
       │                         └── 材料：营业执照 + 法人身份证 + 对公账户
       ▼
  [2] 绑定小程序到商户号（审核通过后）
       │
       ▼
  [3] 设置 APIv3 密钥 + 下载证书
       │
       ▼
  [4] 填写 .env 全部 WXPAY_* 变量 + WECHAT_APP_SECRET
       │
       ▼
  [5] 设置 PAYMENT_MODE=real → 重启服务 → 支付功能可用
```

---

## 四、海容本周优先操作建议

| 优先级 | 事项 | 建议完成时间 |
|:------:|:-----|:-----------:|
| 🔴 **立即** | 准备商户号注册材料（营业执照照片、法人身份证、对公账户信息） | 今天 |
| 🔴 **立即** | 登录 mp.weixin.qq.com → 生成 **WECHAT_APP_SECRET** | 今天 |
| 🟡 **本周** | 提交微信支付商户号注册申请 | 今天（越早越好，审核要等） |
| 🟡 **本周** | 注册 Google Gemini API Key（免费） | 本周内 |
| 🟢 **审核通过后** | 设置 APIv3 密钥 + 下载证书 + 填写全部 .env 变量 | 商户号审核通过后 |

---

## 五、已无需操作的项目

| 变量 | 原因 |
|:-----|:------|
| APP_URL | 已填 `http://localhost:8000`，开发用 |
| VITE_API_BASE | 已填 `/lkapi` |
| SECRET_KEY | 已填开发密钥（生产环境再改） |
| ACCESS_TOKEN_EXPIRE_MINUTES / REFRESH_TOKEN_EXPIRE_DAYS | 已配置 |
| CORS_ORIGINS | 已配 `*` 开发模式 |
| SENTRY_ENVIRONMENT / SENTRY_TRACES_SAMPLE_RATE | 已填默认值 |
| WECHAT_APPID（硬编码） | 代码中已硬编码 `wxb4f6d89904200fd2` |
| PG_* 数据库 | 当前用 SQLite 无需操作 |

---

*生成时间: 2026-05-29 08:11*
*来源: D:/链客宝AI/.env + 代码审计 + 现有SOP文档*
