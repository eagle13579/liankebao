/**
 * 链客宝 Jest 测试设置
 * =============================================================================
 * 此文件在每次测试运行前自动执行，用于配置全局测试环境。
 */

// 导入 @testing-library/jest-dom 扩展匹配器（如 toBeInTheDocument）
import '@testing-library/jest-dom';

// 如果环境需要，可以在此添加全局 mock
// 例如：
// jest.mock('next/router', () => require('next-router-mock'));
