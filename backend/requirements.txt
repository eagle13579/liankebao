# 链客宝后端依赖
# 核心框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# 数据库
sqlalchemy>=2.0.0
pymysql>=1.0.0

# YAML 规则解析
pyyaml>=6.0

# CORS 支持
# (fastapi 内置 CORSMiddleware)

# 测试
pytest>=8.0.0
httpx>=0.27.0

# 数据库迁移
alembic>=1.13.0

# 工具
python-multipart>=0.0.12
