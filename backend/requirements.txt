fastapi==0.115.0
uvicorn==0.30.0
sqlalchemy==2.0.35
pydantic==2.9.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
httpx==0.27.0
pymysql==1.1.1
cryptography==43.0.0

# 中文分词（搜索引擎）
jieba==0.42.1

# PostgreSQL 驱动（可选 — 仅迁移到 PG 时需要）
# 安装: pip install psycopg2-binary
psycopg2-binary==2.9.9

# PostgreSQL驱动（多租户模式需要）
psycopg2-binary==2.9.9

# Sentry 错误追踪（可选 — 生产环境建议安装）
# 安装: pip install sentry-sdk
# sentry-sdk==2.20.0

# PostHog 用户行为分析埋点（可选 — 设置 POSTHOG_API_KEY 环境变量启用）
# 安装: pip install posthog
posthog==3.21.0

# 告警管理与自动恢复依赖
psutil==7.2.2
requests==2.32.3

# 企业信息采集（公域数据抓取）
beautifulsoup4==4.12.3
lxml==5.3.0
