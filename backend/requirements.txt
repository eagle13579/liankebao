fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
openai>=1.6.0
python-docx>=1.1.0
openpyxl>=3.1.2
PyPDF2>=3.0.0
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-cov>=5.0.0
httpx>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── Sentry 错误追踪 ──────────────────────────────────────────────
sentry-sdk>=2.0.0

# ── Celery + Redis 消息队列 ─────────────────────────────────────────
celery[redis]>=5.4.0

# ── Prometheus 监控指标 ────────────────────────────────────────────
prometheus-client>=0.19.0
prometheus-fastapi-instrumentator>=6.1.0

# ── SSR 预渲染 (Puppeteer 动态渲染) ─────────────────────────────
pyppeteer>=2.0.0
