name: payment
version: "1.0.0"
description: 支付管理
author: 链客宝
dependencies:
  - auth
  - orders
routes:
  prefix: /api/payment
  auth_required: true
frontend:
  entry: ./frontend/PaymentModule
  routes:
    - /payment
    - /payment/methods
permissions:
  - payment.pay
  - payment.refund
  - payment.methods
