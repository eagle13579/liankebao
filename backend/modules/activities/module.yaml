name: activities
version: "1.0.0"
description: 联系人活动管理
author: 链客宝
dependencies:
  - auth
  - contacts
routes:
  prefix: /api/contacts/{contact_id}/activities
  auth_required: true
permissions:
  - activities.view
  - activities.create
