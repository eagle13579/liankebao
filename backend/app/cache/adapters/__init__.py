"""Cache adapter implementations — Interface + Adapter pattern.

This package contains concrete implementations of cache interfaces.
Each adapter wraps a specific caching backend (in-memory dict, Redis, etc.).
"""
