"""Repository adapter implementations — Interface + Adapter pattern.

This package contains concrete implementations of repository interfaces.
Each adapter wraps a specific backend (SQLAlchemy, Redis, MongoDB, etc.)
and translates interface calls into backend-specific operations.
"""
