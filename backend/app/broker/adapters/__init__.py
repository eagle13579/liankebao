"""Broker adapter implementations — Interface + Adapter pattern.

This package contains concrete implementations of service broker interfaces.
Each adapter implements inter-service communication for a specific transport.
"""
